{
  "name": "react-tetris-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "npm install && rm -rf build && cd client/ && npm install && CI=false npm run build --if-present && cp -r ./build ../build",
    "start": "node index.js",
    "build": "rm -rf build && cd client/ && npm run build && cp -r ./build ../build",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mikko Syrjälä",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
